@model NGTI.Models.SoloReservation
@*
*@
@{
    string id = (string)ViewContext.RouteData.Values["id"];
}

<h1>delete here reservation based on ID given with the button click.</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>reservering</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IdSoloReservation)
        </dt>
        <dd class="col-sm-10">
            @id
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
            TEST-NAME
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Reservations">Back to List</a>
    </form>
</div>
