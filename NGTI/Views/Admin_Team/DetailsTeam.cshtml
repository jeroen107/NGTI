@model IEnumerable<Employee>

@{
    ViewData["Title"] = "DeleteTeam";
    string name = (string)TempData["name"];
}
<head>
    <link rel="stylesheet" href="~/css/DetailsTeam.css" />
</head>
<div>
    <h4>Team details</h4>
    <hr />
    <dl class="row m25">
        <dt class="col-sm-2">
            Team Name
        </dt>
        <dd class="col-sm-10">
            @name
        </dd>
    </dl>
    <table class="content-table m25">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BHV)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Admin)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (Employee item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BHV)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Admin)
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div>
        <a asp-action="EditTeam" asp-route-name="@name">Edit</a> |
        <a asp-controller="Admin_Team" asp-action="Overview">Back to List</a>
    </div>
</div>
